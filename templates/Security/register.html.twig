{% extends 'base.html.twig' %}

{% block page_title 'Register' %}
{% block title 'Register' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {#
        FIXME:
        Affiche le formulaire d'inscription avec les champs
        firstname
        lastname
        email
        password (répété)
        birthday

    #}
    <input type="submit" value="register" />
{% endblock %}
